# Frontend (Next.js) — Environment Variables Example
# Copy to ".env.local" and adjust for your machine.

# API base (NextAuth endpoint lives under this host)
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:3004

# Site password used by PasswordGate (client-visible). Do NOT use secrets here.
NEXT_PUBLIC_SITE_PASSWORD=きぼう

# Development helper: skip auth checks when true
NEXT_PUBLIC_SKIP_AUTH=false

# Optional: If you really need to expose the app URL to client-side code.
#NEXT_PUBLIC_APP_ORIGIN=http://127.0.0.1:3000

